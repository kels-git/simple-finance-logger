//Interface - allows us to enforce a certain class /structure
//Interface can be used to describe what properties and what methods and
//what the type of those properties and return types of methods are

export interface HasFormatter {
  format(): string;
}
